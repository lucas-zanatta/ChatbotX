"use client"

import type { EmailSpacingStepSchema } from "@aha.chat/flow-config"

type EmailSpacingStepViewerProps = {
  data: EmailSpacingStepSchema
}

export default function EmailSpacingStepViewer(
  _props: EmailSpacingStepViewerProps,
) {
  return <div> </div>
}
