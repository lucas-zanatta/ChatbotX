export const queueName = {
  integration: "integration",
  chat: "chat",
  aiAgent: "aiAgent",
  schedule: "schedule",
  trigger: "trigger",
  webhook: "webhook",
  default: "default",
  analytics: "analytics",
} as const
